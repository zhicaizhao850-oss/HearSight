# ASR Backend - 本地版本
# 包含完整的本地模型支持，包括 torch 等大型依赖
# 本地版本可以作为备选方案支持云端版本

fastapi
uvicorn
pydantic
pydantic-settings
python-multipart
pytest
pytest-asyncio
httpx
requests

# 本地 ASR 模型依赖
funasr>=0.4.0

# Supabase 支持
supabase

# 深度学习框架 - 在 Dockerfile 中单独安装 CUDA 版本
# torch==2.1.0+cu118
# torchaudio==2.1.0+cu118

# 语言检测
langdetect
textblob

# 可选：云端 API 支持（如果想在本地版本中也支持云端作为备选）
dashscope

# 环境变量支持
python-dotenv>=1.0.0
